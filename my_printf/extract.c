#include "stdio.h"

/**
 * exact_p - Calculates the precision for printing
 *
 * Return: Precision.
 */
int exact_p(const char *format, int *i, va_list list)
{
	int val_i = *i + 1;
	int precison = - 1;

	if (format[val_i] != '.')
	{
		return (precision)
	} 
